[package]
name = "kalvita"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Kalvita — a small array-first scripting language"

[dependencies]
rusqlite = { version = "0.40.2", features = ["bundled"] }
serde_json = "1.0.151"
ureq = "3.4.2"
